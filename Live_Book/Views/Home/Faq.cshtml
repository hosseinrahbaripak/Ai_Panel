@* @model IEnumerable<Ai_Panel.Domain.FrequentlyQuestion> *@
@{
    Layout = "_AppLayout";
    ViewData["Title"] = "سوالات متداول | کتابخوان علامه";
    string error = ViewBag.Error;
}
<h4 class="mb-5 mt-5 text-center">سوالات متداول</h4>

@* @if (!string.IsNullOrEmpty(error))
{
    <div class="alert alert-info">
        <p>@error</p>
    </div>
}
else
{
    @foreach (var item in Model)
    {
        <div class="mt-3 mb-4 alert alert-info" style="text-align: justify;">
            <h5 class="mb-2">@item.Question</h5>
            <p class="text-justify">@item.Answer</p>
        </div>
    }
}
 *@
